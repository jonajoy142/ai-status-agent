services:
  - type: web
    name: sprintpilot-ai-api
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: LLM_PROVIDER
        value: demo
      - key: VECTOR_PROVIDER
        value: local
